# document and style  using reusable workflow
on:
  workflow_dispatch:
  push:
name: call-doc-and-style-r
jobs:
  call-workflow:
    uses: nmfs-fish-tools/ghactions4r/.github/workflows/doc-and-style-r.yml@docandstyle-deps
    with: 
      run-rm_dollar_sign: true
