# document and style using reusable workflow
on:
  workflow_dispatch:
  push:
  pull_request:
    paths:
      - 'R/**'
      - 'tests/**'
      - 'vignettes/**'
name: call-doc-and-style-r
jobs:
  call-workflow:
    uses: nmfs-fish-tools/ghactions4r/.github/workflows/doc-and-style-r.yml@quotes_dollar_sign_fix
    with: 
      run-rm_dollar_sign: true
      commit-directly: true