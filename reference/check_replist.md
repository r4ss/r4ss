# Check if the replist input is something that was created by SS_output()

Check if the replist input is something that was created by SS_output()

## Usage

``` r
check_replist(replist)
```

## Arguments

- replist:

  An object to test
